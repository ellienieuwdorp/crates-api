--- specs/1.15.1/openapi.yaml
+++ specs/1.15.3-beta.1/openapi.yaml
@@ -3,7 +3,7 @@
   title: Crates API
   description: REST API surface of the Crates DJ music library app server. This spec is auto-generated by deterministically
     parsing the decompiled JAX-RS + Swagger annotations from Crates.app.
-  version: 1.15.1
+  version: 1.15.3-beta.1
   contact:
     name: Generated from Crates.app decompilation
 servers:
@@ -1247,7 +1247,7 @@
       tags:
       - audiofiles
       summary: Relocate the path of all audioFiles starting with old_path
-      description: returns the number of updated files
+      description: queues a relocate task and returns its task id
       operationId: audiofiles_relocateAudioFiles_audiofiles_relocate_all
       parameters:
       - name: old_path
@@ -1357,6 +1357,29 @@
         '500':
           description: Internal server error
       security: []
+  /backend/callback-owner:
+    get:
+      tags:
+      - backend
+      summary: Checks callback ownership for localhost forwarding probes
+      operationId: backend_callbackOwner_backend_callback_owner
+      parameters:
+      - name: provider
+        in: query
+        required: false
+        schema:
+          type: string
+      - name: callbackKey
+        in: query
+        required: false
+        schema:
+          type: string
+      responses:
+        '200':
+          description: Checks callback ownership for localhost forwarding probes
+        '500':
+          description: Internal server error
+      security: []
   /backend/shutdown:
     get:
       tags:
@@ -3243,6 +3266,45 @@
           description: Exception's message
       security:
       - ClientID: []
+  /crates/update.batch:
+    post:
+      tags:
+      - crates
+      summary: Refresh multiple crates
+      description: Queues a single background task that refreshes the requested crates sequentially.
+      operationId: crates_refreshCratesBatch_crates_update_batch
+      parameters:
+      - name: Client-ID
+        in: header
+        description: The id of the client who requested the invocation of this service
+        required: false
+        schema:
+          type: string
+      - name: Callbacks
+        in: header
+        description: 'The flag to indicate whether or not to send callback via websocket. Allow type: ''CALLBACKS_OFF'' and
+          ''CALLBACKS_ON'''
+        required: false
+        schema:
+          type: string
+      requestBody:
+        content:
+          application/json:
+            schema:
+              type: string
+              description: Batch refresh request
+        required: true
+      responses:
+        '200':
+          description: Refresh multiple crates
+        '202':
+          description: accepted
+        '400':
+          description: Bad Request
+        '500':
+          description: Exception's message
+      security:
+      - ClientID: []
   /crates/tabs:
     get:
       tags:
@@ -3628,6 +3690,8 @@
                 $ref: '#/components/schemas/CrateTunesPresentationBean'
         '400':
           description: Bad Request
+        '404':
+          description: Object not found
         '500':
           description: Internal Server Error
       security:
@@ -4598,6 +4662,11 @@
         required: true
         schema:
           type: string
+      - name: state
+        in: query
+        required: false
+        schema:
+          type: string
       responses:
         '200':
           description: Successful response
@@ -5125,6 +5194,43 @@
           description: Image not found
         '500':
           description: Internal Server Error
+      security:
+      - ClientID: []
+  /images/regenerate:
+    post:
+      tags:
+      - images
+      summary: Regenerate generated image for an object
+      operationId: images_regenerateImageForObject_images_regenerate
+      parameters:
+      - name: objectType
+        in: query
+        description: the type of the object
+        required: true
+        schema:
+          type: string
+      - name: objectID
+        in: query
+        description: the ID of the object
+        required: true
+        schema:
+          type: integer
+          format: int64
+      responses:
+        '200':
+          description: Regenerate generated image for an object
+          content:
+            application/json:
+              schema:
+                type: array
+                items:
+                  $ref: '#/components/schemas/ImageToObjectPresentationBean'
+        '400':
+          description: Bad Request
+        '404':
+          description: Not Found
+        '500':
+          description: Exception's message
       security:
       - ClientID: []
   /images/object:
@@ -7525,6 +7631,13 @@
         required: false
         schema:
           type: string
+      - name: parent_crate_id
+        in: query
+        description: Optional parent crate id to attach the imported playlist under
+        required: false
+        schema:
+          type: integer
+          format: int64
       - name: Client-ID
         in: header
         required: false
@@ -8359,7 +8472,7 @@
       tags:
       - releases
       summary: Delete Release by ID
-      description: Deletes a release from the database by its ID.
+      description: Deletes a release entity by its ID while keeping the underlying tunes in the library.
       operationId: releases_deleteRelease_releases_id
       parameters:
       - name: id
@@ -8952,6 +9065,11 @@
         in: query
         description: the code
         required: true
+        schema:
+          type: string
+      - name: state
+        in: query
+        required: false
         schema:
           type: string
       responses:
@@ -9276,6 +9394,38 @@
         '500':
           description: Internal server error
       security: []
+  /sync/export.decision:
+    post:
+      tags:
+      - sync
+      summary: sync_submitExportDecision_sync_export_decision
+      operationId: sync_submitExportDecision_sync_export_decision
+      requestBody:
+        content:
+          application/json:
+            schema:
+              type: string
+        required: false
+      responses:
+        '200':
+          description: Successful response
+        '500':
+          description: Internal server error
+      security:
+      - ClientID: []
+  /sync/cancel:
+    post:
+      tags:
+      - sync
+      summary: sync_cancelSync_sync_cancel
+      operationId: sync_cancelSync_sync_cancel
+      responses:
+        '200':
+          description: Successful response
+        '500':
+          description: Internal server error
+      security:
+      - ClientID: []
   /sync/export.backup:
     get:
       tags:
@@ -11340,6 +11490,38 @@
           description: Exception's message
       security:
       - ClientID: []
+  /tunes/crates/preload:
+    post:
+      tags:
+      - tunes
+      summary: Preload tunes for crates
+      description: Schedules a background preload of crate tunes to warm caches.
+      operationId: tunes_preloadTunesForCrates_tunes_crates_preload
+      parameters:
+      - name: stickySubscription
+        in: query
+        description: if true, crates are added as sticky subscriptions
+        required: false
+        schema:
+          type: string
+      requestBody:
+        content:
+          application/json:
+            schema:
+              type: string
+              description: a list of crate IDs
+        required: true
+      responses:
+        '200':
+          description: Preload tunes for crates
+        '202':
+          description: accepted
+        '400':
+          description: Bad Request
+        '500':
+          description: Exception's message
+      security:
+      - ClientID: []
   /tunes/crates/{crate_id}:
     get:
       tags:
@@ -11917,9 +12099,11 @@
       tags:
       - tunes
       summary: Adds a list of existing tunes to a crate
-      description: 'Adds a list of existing tunes to a crate, indicated by its id. Tunes are represented by a list of tuneIDs.
-        It returns the new ordered list of tunes that now belongs to that crateExample: [ {\"tuneID\" : 5}, {\"tuneID\": 15}]
-        Returns the list of the crate''s tunes'
+      description: 'Adds a list of existing tunes to a crate, indicated by its id. When header ''CALLBACKS'' is ''CALLBACKS_OFF'',
+        the response blocks until the returned crate tune list reflects the completed add. When header ''CALLBACKS'' is ''CALLBACKS_ON'',
+        websocket callbacks may deliver the final update asynchronously. Tunes are represented by a list of tuneIDs. It returns
+        the new ordered list of tunes that now belongs to that crateExample: [ {\"tuneID\" : 5}, {\"tuneID\": 15}] Returns
+        the list of the crate''s tunes'
       operationId: tunes_addTunesToCrate_tunes_crates_crate_id_add
       parameters:
       - name: crate_id
@@ -12030,8 +12214,10 @@
       tags:
       - tunes
       summary: Removes a list of existing tunes from a crate
-      description: 'Removes a list of existing tunes from a crate, indicated by its id. It returns the new ordered list of
-        tunes that now belongs to that crate Tunes are represented by a list of tuneIDs. Example: [ {\"tuneID\" : 5}, {\"tuneID\":
+      description: 'Removes a list of existing tunes from a crate, indicated by its id. When header ''CALLBACKS'' is ''CALLBACKS_OFF'',
+        the response blocks until the returned crate tune list reflects the completed removal. When header ''CALLBACKS'' is
+        ''CALLBACKS_ON'', websocket callbacks may deliver the final update asynchronously. It returns the new ordered list
+        of tunes that now belongs to that crate Tunes are represented by a list of tuneIDs. Example: [ {\"tuneID\" : 5}, {\"tuneID\":
         15}] Returns the list of the crate''s tunes'
       operationId: tunes_removeTunesFromCrate_tunes_crates_crate_id_remove
       parameters:
@@ -15274,6 +15460,12 @@
         tabCrateList:
           $ref: '#/components/schemas/TabCrateList'
           description: contains the tabs of crates
+        stickySubscribedCrateIds:
+          type: array
+          items:
+            type: integer
+            format: int64
+          description: crate IDs to keep as sticky subscriptions
         allowServerDiscovery:
           type: boolean
           description: allow to register to local jmDNS to be discoverable by a mobile device on the same local network
@@ -15374,6 +15566,12 @@
         DJProfileEnabled:
           type: boolean
           description: if dj profile is enabled
+        advancedProfileEnabled:
+          type: boolean
+          description: if advanced profile is enabled
+        iTunesProfileEnabled:
+          type: boolean
+          description: if iTunes profile/task is enabled
         pluginSettings:
           type: object
           additionalProperties:
